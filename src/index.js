import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <div>
            <h1>Build My PC</h1>
            <p>This is a placeholder deployment. The real application is in the /frontend directory.</p>
        </div>
    </React.StrictMode>
);
